<%= render 'main'%>
<br /></br /><br />

    <% @categories.each do |category|%>

    <ul class="flex gap-10 flex-wrap justify-center">
        <li>
            <div class="max-w-sm rounded overflow-hidden shadow-lg">
                <div class="px-6 py-4">
                    <div class="font-bold text-xl mb-2"><%= category.name %></div>
                    <div><%= category.created_at.strftime('%a %d %b %Y')%>&nbsp; <%= category.created_at.strftime("at %I:%M%p")%></div>
                    <p>task count</p>
                
                </div>
                <div class="px-6 pt-4 pb-2 flex justify-center gap-10">
                    <%= link_to '👓view', '#', class: 'bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2' %>
                    <%= link_to '⚙ edit', '#', class: 'bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2' %>
                </div>
            </div>
        </li>

    </ulf>

    <% end %>
