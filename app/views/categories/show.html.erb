<div class="h-screen flex items-center justify-center p-5 w-screen">

<div class="flex flex-col gap-5">
        <div class="max-w-sm rounded overflow-hidden shadow-lg">
                <div class="px-6 py-4">
                        <div class="font-bold text-xl mb-2 text-center"><%= @category.name%></div>
                        <p class="text-gray-700 text-base">
                        <% @tasks = Task.where("category_id = ?", @category.id) %>
                                <table>
                                <thead>
                                        <tr>
                                        <td></td>
                                        <td colspan="3"></td>
                                        </tr>
                                </thead>
                                <tbody>
                                <% @tasks.each do |task| %>
                                        <tr>
                                        <td class="p-3"><%= task.name.downcase%></td>
                                        <td>
                                                <%= link_to '👓', category_task_path(@category.id, task.id), class: 'inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2'%>
                                        </td>
                                        <td>
                                                <%= link_to '⚙', edit_category_task_path(@category.id, task.id), class: 'inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2'%>
                                        </td>
                                        <td>
                                                <%= link_to '❌', delete_task_path(@category.id, task.id), class: 'inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2', data: {confirm: 'Are you sure?'}, method: :delete %>
                                        </td>
                                        </tr>
                                <% end %>
                                </tbody>       
                                </table>
                                        
                        </p>    
                        </div>
                </div>
                <div class="flex justify-center gap-5">
                <%= link_to '➕task', new_category_task_path(@category.id), class: 'bg-green-700 p-2 rounded text-white hover:bg-red-700 text-white' %>
                <%= link_to '👈 categories', categories_path, class: 'bg-green-700 p-2 rounded text-white hover:bg-red-700 text-white' %>
                </div>
        </div>
</div>